// Hacking the Random Generator

#include <stdio.h>

int main() {
	int T; scanf("%d", &T);
	
	while(T--){
		int N, K; scanf("%d %d", &N, &K);
		int A[K], B[K];
		for(int i = 0; i < N; i++) scanf("%d", &A[i]);
		for(int i = 0; i < N; i++) scanf("%d", &B[i]);
	}
}