// Marathon

#include <stdio.h>
#include <cmath>

using namespace std;

int main() {
  freopen("slowdown.in", "r", stdin);
  freopen("slowdown.out", "w", stdout);

  int N; scanf("%d", &N);


  fclose(stdin);
  fclose(stdout);
}
