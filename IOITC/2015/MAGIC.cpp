#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int orig_pos[1000001];

int main() {
  ios::sync_with_stdio(false); cin.tie(0);

  int N, M; cin >> N >> M;

  while(M--) {
    int x, y; cin >> x >> y;

  }
}
