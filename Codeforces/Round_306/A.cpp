#include <iostream>
#include <string>

using namespace std;

int main() {
  string s; cin >> s;
  int N = s.length();

  bool abba = s.find("ABBA");
  bool baab = s.find("BAAB");
  if(abba || baab) cout << "YES" << endl;
  else {
    int ab_first = -1, ba_first = -1;
      if(s[i] == 'A' && s[i + 1])
    for(int i = 0; i < (N - 1); i++){
    }
  }
}
