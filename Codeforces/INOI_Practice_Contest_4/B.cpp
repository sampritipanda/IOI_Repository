#include <iostream>
#include <algorithm>

using namespace std;

#define MOD 1000000007

string S;
int N;

// 0, 1, 2, 3 = bomb, 4 = unknown

int solve(int i, int j) {
  if(i == N) return 1;
}

int main() {
  ios::sync_with_stdio(false);
  cin.tie(0);

  cin >> S;
  N = S.length();
}
